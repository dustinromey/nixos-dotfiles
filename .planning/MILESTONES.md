# Milestones

## v1.0 Voxtype Integration (Shipped: 2026-03-22)

**Phases completed:** 4 phases, 5 plans, 0 tasks

**Key accomplishments:**
- Voxtype flake input pinned to v0.6.4 with NixOS + Home Manager modules imported
- Voxtype daemon running on all hosts with evdev push-to-talk (Alt_R + Menu)
- All waystt artifacts atomically removed (overlay, scripts, keybindings)
- Vulkan GPU acceleration on AMD hosts (intrepid, vigilant), CPU-only on Intel (mischief)
- Waybar STT module showing live voxtype recording/idle status
- Claude Desktop + CLI installed on all hosts via shared common config

---

