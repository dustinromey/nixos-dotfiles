---
phase: 02-daemon-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hosts/common/home.nix
  - config/niri/config.kdl
  - config/waybar/config.jsonc
  - config/waybar/scripts/stt-status.sh
  - bin/stt-toggle.sh
autonomous: true
requirements: [STT-01, STT-02, STT-03, STT-04, PTT-01, PTT-02, PTT-03, REM-01, REM-02, REM-03, REM-04]

must_haves:
  truths:
    - "Voxtype systemd user service is defined and will start on graphical-session.target after home-manager switch"
    - "Voxtype is configured with base.en model, evdev push-to-talk (MENU + RIGHTALT), and type output mode"
    - "PATH workaround for bug #253 is applied so wtype/ydotool are found at runtime"
    - "No waystt references remain in Niri config, waybar config, or bin/ scripts"
    - "No waystt references remain in flake.nix (Phase 1 regression confirmed clean)"
    - "bin/stt-toggle.sh does not exist"
    - "config/waybar/scripts/stt-status.sh does not exist"
    - "nix flake check passes with all changes applied"
  artifacts:
    - path: "hosts/common/home.nix"
      provides: "Voxtype HM module configuration with service, hotkey, model, and PATH workaround"
      contains: "programs.voxtype"
    - path: "config/niri/config.kdl"
      provides: "Niri keybindings without waystt entries"
    - path: "config/waybar/config.jsonc"
      provides: "Waybar config without custom/stt module"
  key_links:
    - from: "hosts/common/home.nix"
      to: "programs.voxtype.service.enable"
      via: "HM module creates systemd user service"
      pattern: "service\\.enable\\s*=\\s*true"
    - from: "hosts/common/home.nix"
      to: "systemd.user.services.voxtype"
      via: "PATH environment override for bug #253"
      pattern: "systemd\\.user\\.services\\.voxtype"
---

<objective>
Configure the voxtype Home Manager module to run as a systemd user service with evdev push-to-talk, and atomically remove all waystt artifacts (Niri keybindings, waybar module, toggle/status scripts).

Purpose: Completes the STT system swap from waystt to voxtype. After this plan, all three hosts have a working speech-to-text daemon with push-to-talk and zero dead waystt references.

Output: Modified hosts/common/home.nix with full voxtype config; cleaned config/niri/config.kdl, config/waybar/config.jsonc; deleted bin/stt-toggle.sh and config/waybar/scripts/stt-status.sh.
</objective>

<domain_assumptions>
**Confirmed rules (from DOMAIN.md):**
- lib.mkDefault in common config: voxtype package already uses lib.mkDefault (Phase 1); no additional mkDefault needed for service/hotkey settings since those are uniform across hosts
- Config symlinks: waybar and niri configs live in config/ and are symlinked to ~/.config/ by home-manager; edits to config/ files take effect on next home-manager switch
- Formatting: All modified Nix files must pass nixfmt-rfc-style
- NEVER put application configs inline in Nix when they can be symlinked: voxtype's config.toml is generated by the HM module (programs.voxtype.settings), which is the intended pattern for module-managed config

**Unconfirmed assumptions (not in DOMAIN.md):**
- The physical Menu key on all three keyboards reports as KEY_MENU (evdev name "MENU"); if any host reports KEY_COMPOSE instead, a per-host override will be needed (Phase 3 or post-deployment fix)
- wtype is bundled inside the voxtype derivation (not a separate system package): the PATH workaround in the systemd service Environment points to the voxtype store path, which is assumed to contain the wtype binary. This assumption is confirmed by checking `nix path-info` on the voxtype package during Task 1 verification.
</domain_assumptions>

<execution_context>
@/home/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/home/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flake-foundation/01-01-SUMMARY.md
@hosts/common/home.nix
@config/niri/config.kdl
@config/waybar/config.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure voxtype HM module with daemon, hotkey, and PATH workaround</name>
  <files>hosts/common/home.nix</files>
  <action>
Add the full voxtype Home Manager module configuration to hosts/common/home.nix. Place the `programs.voxtype` block after the existing imports section (the HM module import from Phase 1 is already in the imports list). Add the systemd PATH workaround as a separate top-level attribute.

Add this configuration (integrate into the existing file structure):

```nix
programs.voxtype = {
  enable = true;
  package = inputs.voxtype.packages.${pkgs.system}.default;

  service.enable = true;

  model.name = "base.en";

  settings = {
    hotkey = {
      enabled = true;
      key = "MENU";
      modifiers = [ "RIGHTALT" ];
      mode = "push_to_talk";
    };
    output = {
      mode = "type";
      fallback_to_clipboard = true;
    };
    whisper = {
      language = "en";
    };
  };
};

# Bug #253 workaround: systemd user service lacks PATH for output tools
systemd.user.services.voxtype = {
  Service = {
    Environment = [ "PATH=/run/current-system/sw/bin:${inputs.voxtype.packages.${pkgs.system}.default}/bin" ];
  };
};
```

Important notes:
- Use `programs.voxtype.package` NOT `lib.mkDefault` here — the NixOS module in common/configuration.nix already sets the system package with mkDefault; this HM-level package must match. Keep it as a direct assignment.
- Do NOT set `settings.whisper.model` — only use `model.name = "base.en"` (the module has an assertion preventing both from being set simultaneously).
- The `systemd.user.services.voxtype` block is an additive merge — the HM module does not set Environment, so no mkForce needed.
- Run `nixfmt-rfc-style hosts/common/home.nix` after editing.
  </action>
  <verify>
Run `nixfmt-rfc-style --check hosts/common/home.nix` (exits 0 = formatted).
Run `nix flake check` (exits 0 = valid configuration for all three hosts).
Grep for `programs.voxtype` in hosts/common/home.nix to confirm the block exists.
Grep for `systemd.user.services.voxtype` in hosts/common/home.nix to confirm PATH workaround exists.
Confirm wtype is bundled in the voxtype derivation: run `nix path-info --recursive $(nix eval --raw .#homeManagerConfigurations.dustin.activationPackage) | xargs -I{} find {} -name wtype 2>/dev/null | head -5` — OR — run `nix build .#packages.x86_64-linux.default --no-link --print-out-paths 2>/dev/null | xargs -I{} ls {}/bin/wtype` to verify the binary exists in the voxtype store path. If wtype is NOT present in the voxtype store path, the PATH workaround must be updated to include the wtype package explicitly (e.g., add `${pkgs.wtype}/bin` to the Environment PATH string).
  </verify>
  <done>
hosts/common/home.nix contains programs.voxtype block with: enable = true, service.enable = true, model.name = "base.en", hotkey settings (MENU + RIGHTALT, push_to_talk), output settings (type mode with clipboard fallback), whisper language = "en". Systemd PATH workaround is present. nix flake check passes for all three hosts. wtype binary location confirmed: either bundled in voxtype store path (PATH workaround sufficient as written) or noted as requiring explicit pkgs.wtype addition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove all waystt artifacts from Niri, Waybar, and scripts</name>
  <files>config/niri/config.kdl, config/waybar/config.jsonc, bin/stt-toggle.sh, config/waybar/scripts/stt-status.sh</files>
  <action>
Remove all waystt references across four locations:

1. **config/niri/config.kdl** — Delete lines 80-82 (the three lines: comment "// Speech to Text (waystt)", the Mod+R bind, and the Mod+Shift+R bind). Leave surrounding lines intact. Do NOT add replacement keybindings — voxtype uses evdev, not compositor binds.

2. **config/waybar/config.jsonc** — Two changes:
   a. Remove `"custom/stt"` from the `modules-right` array (line 12). The array should become: `["pulseaudio", "network", "bluetooth", "battery", "tray"]`
   b. Delete the entire `"custom/stt"` module definition block (lines 92-98, from `"custom/stt": {` through its closing `}`). Also remove the trailing comma from the previous block if needed to keep valid JSONC.

3. **bin/stt-toggle.sh** — Delete this file entirely (`rm bin/stt-toggle.sh`).

4. **config/waybar/scripts/stt-status.sh** — Delete this file entirely (`rm config/waybar/scripts/stt-status.sh`). If the scripts/ directory is empty after deletion, leave it (other scripts may be added later).
  </action>
  <verify>
Grep for "waystt" in config/niri/config.kdl — should return no matches.
Grep for "stt-toggle" in config/niri/config.kdl — should return no matches.
Grep for "custom/stt" in config/waybar/config.jsonc — should return no matches.
Confirm bin/stt-toggle.sh does not exist: `test ! -f bin/stt-toggle.sh`.
Confirm config/waybar/scripts/stt-status.sh does not exist: `test ! -f config/waybar/scripts/stt-status.sh`.
Run `nix flake check` to confirm no broken references.
REM-01/REM-02 regression check (Phase 1 facts confirmed clean): `grep -r waystt flake.nix` must return no matches; `test ! -d packages/waystt` must succeed (directory must not exist).
  </verify>
  <done>
No waystt references remain in Niri config (Mod+R and Mod+Shift+R binds removed). Waybar config has no custom/stt module in modules-right or module definitions. bin/stt-toggle.sh and config/waybar/scripts/stt-status.sh are deleted. nix flake check still passes. Phase 1 regression confirmed: flake.nix contains no waystt string, packages/waystt/ directory does not exist.
  </done>
</task>

</tasks>

<verification>
1. `nix flake check` passes — no evaluation errors from voxtype config or missing file references
2. `grep -r "programs.voxtype" hosts/common/home.nix` shows the full configuration block
3. `grep -r "systemd.user.services.voxtype" hosts/common/home.nix` shows PATH workaround
4. `grep -r "waystt\|stt-toggle\|stt-status\|custom/stt" config/ bin/` returns no matches
5. `nixfmt-rfc-style --check hosts/common/home.nix` exits 0
6. `grep -r waystt flake.nix` returns no matches (REM-01/REM-02 regression clean)
7. `test ! -d packages/waystt` succeeds (REM-01/REM-02 regression clean)
8. `test ! -f bin/stt-toggle.sh` succeeds
9. `test ! -f config/waybar/scripts/stt-status.sh` succeeds
</verification>

<success_criteria>
- Voxtype HM module fully configured: service enabled, base.en model, evdev PTT with MENU+RIGHTALT, type output with clipboard fallback, PATH workaround for bug #253
- All waystt artifacts removed: Niri keybindings, waybar module, toggle script, status script
- Phase 1 regression confirmed: no waystt in flake.nix, packages/waystt/ absent
- nix flake check passes for all three hosts (mischief, intrepid, vigilant)
- All Nix files formatted with nixfmt-rfc-style
</success_criteria>

<output>
After completion, create `.planning/phases/02-daemon-replacement/02-01-SUMMARY.md`
</output>