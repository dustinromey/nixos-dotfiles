---
phase: 04-add-claude-for-linux-to-nixos-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - hosts/common/home.nix
autonomous: true
requirements:
  - CLAUDE-01
  - CLAUDE-02
  - CLAUDE-03
  - CLAUDE-04

must_haves:
  truths:
    - "claude --version works after rebuild (CLI available in PATH)"
    - "Claude desktop app appears in desktop entries and can launch from Rofi"
    - "nix flake check passes with both claude packages present"
    - "All hosts share the same claude installation via common home.nix"
  artifacts:
    - path: "flake.nix"
      provides: "claude-desktop and flake-utils inputs"
      contains: "claude-desktop.url"
    - path: "hosts/common/home.nix"
      provides: "claude-desktop package in home.packages"
      contains: "claude-desktop"
  key_links:
    - from: "flake.nix"
      to: "hosts/common/home.nix"
      via: "inputs.claude-desktop passed through extraSpecialArgs"
      pattern: "inputs\\.claude-desktop\\.packages"
---

<objective>
Add Claude Desktop Linux app to the NixOS configuration alongside the already-installed Claude Code CLI.

Purpose: Complete the claude-for-linux setup so both the CLI and desktop app are available on all hosts via shared common configuration.
Output: Updated flake.nix with claude-desktop input, updated home.nix with desktop package reference.
</objective>

<domain_assumptions>
**Confirmed rules (from DOMAIN.md):**
- Flake inputs: New external flakes added as inputs in flake.nix and threaded through via specialArgs/extraSpecialArgs
- Config symlinks: No inline app config needed here (k3d3 package handles desktop entry automatically)
- Formatting: All Nix files must pass nixfmt-rfc-style
- lib.mkDefault: Not needed here since claude packages are shared across all hosts with no per-host override

**Unconfirmed assumptions (not in DOMAIN.md):**
- k3d3/claude-desktop-linux-flake's .desktop file will be picked up by Rofi from home.packages install path (test after rebuild)
</domain_assumptions>

<execution_context>
@/home/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/home/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-add-claude-for-linux-to-nixos-setup/04-RESEARCH.md
@flake.nix
@hosts/common/home.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claude-desktop flake input with flake-utils dependency</name>
  <files>flake.nix</files>
  <action>
Add two new inputs to flake.nix in the inputs block, grouped with the existing Claude Code input:

1. `flake-utils` input (required dependency for the desktop flake):
   ```
   flake-utils.url = "github:numtide/flake-utils";
   ```

2. `claude-desktop` input with follows directives:
   ```
   claude-desktop.url = "github:k3d3/claude-desktop-linux-flake";
   claude-desktop.inputs.nixpkgs.follows = "nixpkgs";
   claude-desktop.inputs.flake-utils.follows = "flake-utils";
   ```

Place these immediately after the existing `claude-code` input lines (lines 9-11) under the `# Claude` comment, so all Claude-related inputs are grouped together.

IMPORTANT: Use the standard `claude-desktop` package, NOT `claude-desktop-with-fhs` (FHS variant is only for MCP servers, which is out of scope per user decision).

After editing, run `nixfmt-rfc-style flake.nix` to format, then run `nix flake lock --update-input claude-desktop --update-input flake-utils` to update the lock file with the new inputs.
  </action>
  <verify>Run `nix flake check` — must pass without errors. Verify the lock file contains entries for both claude-desktop and flake-utils.</verify>
  <done>flake.nix has claude-desktop and flake-utils inputs; flake.lock is updated; nix flake check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add claude-desktop package to common home.nix</name>
  <files>hosts/common/home.nix</files>
  <action>
Add the claude-desktop package reference to the `home.packages` list in hosts/common/home.nix. Place it on the line after the existing `claude-code` entry (line 141), using the direct flake input reference pattern (same as the `fresh` package on line 142):

```
inputs.claude-desktop.packages.${pkgs.system}.claude-desktop
```

This follows the identical pattern to `inputs.fresh.packages.${pkgs.system}.default` already in the file, except using the named package `claude-desktop` instead of `default`.

Do NOT add any `xdg.desktopEntries` for Claude Desktop — the k3d3 package auto-includes a .desktop file. Adding one manually would create a duplicate.

After editing, run `nixfmt-rfc-style hosts/common/home.nix` to format.
  </action>
  <verify>
Run `nix flake check` to verify the full configuration is valid.
Run `grep -n "claude" hosts/common/home.nix` to confirm both claude-code and claude-desktop are present in home.packages.
  </verify>
  <done>hosts/common/home.nix includes both claude-code and claude-desktop packages; nix flake check passes; no manual desktop entry was added.</done>
</task>

</tasks>

<verification>
1. `nix flake check` passes — proves CLAUDE-04
2. `grep "claude-desktop" flake.nix` shows the input — proves flake integration
3. `grep "claude-desktop" hosts/common/home.nix` shows the package — proves CLAUDE-02, CLAUDE-03
4. `grep "claude-code" hosts/common/home.nix` confirms CLI still present — proves CLAUDE-01
5. No host-specific files were modified — proves shared via common home.nix (CLAUDE-03)
</verification>

<success_criteria>
- flake.nix contains claude-desktop and flake-utils inputs with proper follows directives
- hosts/common/home.nix contains both claude-code and claude-desktop in home.packages
- nix flake check passes
- No host-specific configuration files were modified (all shared via common)
- Both Nix files pass nixfmt-rfc-style formatting
</success_criteria>

<output>
After completion, create `.planning/phases/04-add-claude-for-linux-to-nixos-setup/04-01-SUMMARY.md`
</output>
